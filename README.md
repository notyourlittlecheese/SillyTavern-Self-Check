# 墨提斯之镜 · SillyTavern 写作前置自检

一个用于 SillyTavern 角色扮演的写作前置自检、预设管理、参考资料注入与临时指令插件。

> 当前正式版本：`0.4.3`

## v0.4.3 更新

- 最新自检结果和运行日志会显示本轮由哪个自检 API 与模型输出。
- 模型多选改为复选列表，手机和电脑都可以直接点选多个模型，不需要 Ctrl/Command。

## v0.4.2 更新

- 主自检 API 和备用自检 API 均支持刷新模型列表并多选模型。
- 同一个 API 配置选择多个模型时，会按选中顺序逐个尝试；失败后再尝试下一个模型或下一个备用 API。
- 旧版单模型配置会自动迁移为多模型列表中的第一项。

## v0.4.1 更新

- 双 API 新增备用自检 API 列表，主接口失败后会按顺序尝试备用接口。
- 每个备用 API 可单独设置接口地址、模型、密钥、启用状态和优先顺序。
- 全部自检 API 都失败时，运行日志会汇总每个接口的失败原因，方便排查是哪条线路不可用。

## v0.4.0 更新

完整迁入 DEV `0.4.0-beta.22` 的功能，并适配正式版的安装目录、配置、输出标签和更新地址。

- 双 API：独立接口完成自检，再交由酒馆主 API 输出正文，支持模型列表、多个备用接口、超时、精简重试与失败回退。
- 上一轮复盘：连续成功的双 API 生成可核对上一轮自检和正文；漏复盘时记录状态，不阻断本轮正文。
- 强力规范：将自检结论转成 YAML，以 System、depth 0 临时注入，正文仍须位于推理标签之外。
- 思维链边界修复：仅在自检提供可靠分界时分开错误包裹的正文；正常推理及酒馆独立保存的 reasoning 数据保留。
- 运行日志：记录单／双 API、题数、正文与复盘结果，错误说明包含具体原因与处理方法。
- 更新检查：GitHub Raw、备用 CDN、GitHub API 多路读取，支持插件内更新及失败诊断。
- 保留资料库、快捷指令、预设导入导出、移动端浮窗与边缘停靠等完整功能。

## 从旧正式版或 DEV 升级

原正式版用户直接更新本仓库即可；插件设置、预设、角色绑定与外观沿用正式版的数据。旧“双阶段严格模式”自动转为单 API，可在设置中另行配置双 API。

如果要继续使用同一酒馆中 DEV 的测试配置：

1. 等待生成结束，更新正式版并刷新页面。
2. 打开“墨提斯之镜 → 插件设置 → 从 DEV 迁入设置”。
3. 确认后会复制 DEV 的预设、角色绑定、资料库、快捷指令、API 配置、日志与外观；自动备份迁入前的正式版设置并立即保存。
4. 正式版启用，DEV 内部开关关闭。请在酒馆扩展页停用 DEV 并刷新页面，之后使用正式版。

如需撤回配置迁入，点击同处的“恢复迁入前的设置”。重复迁入仍保留最初的备份。聊天记录及两版历史自检记录不搬动；API 密钥保存在酒馆设置中，不会加入公开仓库、插件安装包或预设／资料库导出文件。备份也只保存在当前酒馆设置中。

如果 DEV 在另一份酒馆或另一用户档案，迁移按钮无法访问那里的设置；请使用现有预设和资料库导出／导入，再在正式版填写 API 配置。DEV 仓库继续作为开发测试仓库。

## v0.3.4 更新

- 插件设置新增“悬浮按钮大小”，可在 34px—50px 之间即时调整。
- 50px 保持原来的按钮大小，向左拖动可缩小两档以上，步进为 2px。
- 图标、错误红点、拖动安全区域与屏幕边界会跟随按钮大小同步适配。
- 该设置只改变悬浮按钮，不影响打开后的悬浮窗尺寸和内容。

## v0.3.3 更新

- “版本更新”页发现新版本后可直接点击“立即更新”，无需跳转到 SillyTavern 扩展管理器。
- 更新成功后会自动刷新页面，使新代码立即生效；存在未保存设置时会先阻止更新并提醒保存。
- 更新提示改为只比较远程与当前版本号，普通 Git 提交、README 修改或检查状态异常不再触发误报。
- 取消进入酒馆时自动弹出的“已安装／已更新”提示；没有真正的新版本时不再出现任何更新弹窗。
- 同一远程版本只主动弹出一次提示，仍会在魔法棒入口和版本更新页持续显示更新状态。

## v0.3.2 更新

- 批量导入问题的自动识别改为只按换行分隔。
- 每个非空行视为一道完整问题，同一行出现多个问号也不会被拆开。
- 行首序号和项目符号仍会自动清理，识别后仍可预览、编辑和删除。

## v0.3.1 更新

- 修复插件关闭后仍继续检查AI回复，并错误提示“本轮AI未输出自检问答”的问题。
- 关闭插件时会立即清理未完成的自检运行状态和临时注入；之后的回复不会被插件解析或改写。
- 插件关闭期间不显示悬浮按钮错误红点，历史自检记录仍会保留。

## v0.3.0 更新

- 参考资料库新增“批量导出”：可勾选多个资料库，一次导出为一个 `.stsc-references.json` 合集文件。
- 导入资料库时同时兼容单个资料库文件与资料库合集文件；合集只需导入一次即可全部加入。
- 新增“版本更新”页面，可直接查看当前版本、本次更新内容、远程更新状态和新版本说明。
- 插件真正升级到新版本后会弹出一次“已安装／更新”提示；检测到远程新版本时会持续提供前往扩展管理器的入口。

## v0.2.9 更新

- 修复插件可能吞掉原生 `<thinking>`、`<think>`、`<reasoning>` 或其他预设思维链内容的问题。
- 插件不再要求 `<stsc_self_check>` 位于整条回复的绝对最前面；原预设的思维链可以正常位于自检之前，自检只需保持在正文之前。
- 生成结束后只精准删除完整闭合的 `<stsc_self_check>…</stsc_self_check>`，不会删除该标签之前或之后的任何内容。
- 自检标签缺失或未闭合时不再进行破坏性裁切，避免误删思维链与正文；插件只记录格式错误并保留原消息。
- 插件不会读取、覆盖或清空 SillyTavern 消息中的原生 reasoning 数据。

## v0.2.8 更新

- 修复插件有新版本时没有及时显示更新提示的问题。旧版把检查时间保存到设置中，并设置了 6 小时检查间隔与 24 小时通知间隔，连续发布版本时可能直接跳过。
- 现在每次启动 SillyTavern 都会立即检查一次，打开插件完整管理器时也会主动复查，并在页面保持开启时每 30 分钟检查。
- 更新检测改为双重校验：同时检查本地 Git 与远程 `manifest.json` 版本；即使本地 Git 状态异常，只要远程版本号更高也能提示。
- 检测到更新后会显示不会自动消失的“插件有更新｜前往更新”提示，并在左下角魔法棒菜单的插件入口显示“更新”标记。

## v0.2.7 更新

- 内置“默认通用自检”的六道核心题现已全部改为开放问答。
- 前四道原判断题同步改写为规划式问题，要求 AI 具体说明本轮如何执行，而不是只回答“是/否”。
- 已安装旧版的用户更新后会自动迁移；仅处理内置默认预设，不影响用户自行创建的其他预设。

## v0.2.6 更新

- 修复旧用户更新后“角色一致性但不僵化”和“真实人类表达”仍显示为判断题的问题。
- 上一版迁移依赖默认题全文完全一致，遇到旧版本保存产生的空格、标点或文字差异时可能跳过；本版改为按内置预设和题目标题识别。
- 已经运行过 v0.2.5 的用户也会再次执行修复，并将这两道默认题强制迁移为开放问答。
- 仅处理内置“默认通用自检”，不会改动用户自己新建的其他通用或角色预设。

## v0.2.5 更新

- “角色一致性但不僵化”和“真实人类表达”改为开放问答，引导 AI 在生成正文前说明本轮将如何具体演绎，而不是机械回答“是”。
- 已安装旧版的用户更新后，会自动迁移内置“默认通用自检”中的这两道默认题；用户自行修改过的不同文本不会被强制覆盖。
- 插件会定期检查自身 Git 更新；发现新版本时显示“插件有更新”提示，点击提示可直接前往 SillyTavern 扩展管理器更新。
- 更新检查失败不会影响角色扮演或正文生成，也不会弹出错误打扰用户。

## v0.2.4 更新

- “透明度”现在只控制圆形悬浮按钮的背景，不再改变打开后的悬浮面板透明度；悬浮窗样式和尺寸设置保持不变。
- 自检缺失或格式错误的提示仅显示在悬浮按钮右上角，不再向聊天正文或消息楼层插入红点/警告标记。
- 打开悬浮窗的“自检问答”页或完整管理器的“当前状态”页后，悬浮按钮提示会立即标记为已读并消失；错误详情仍保留在最新一轮自检中。

## v0.2.3 更新

- 内置新的“默认通用自检”预设，包含：固定格式完整性、角色信息边界与上帝视角、情绪强度与占有欲、关系阶段与亲密行为、角色一致性但不僵化、真实人类表达。
- 更新后会自动把该预设加入“通用预设”列表；仅当用户仍在使用未修改的旧初始默认预设时，才会自动切换为新版，避免覆盖自定义预设。
- 悬浮窗支持自定义样式、背景透明度、宽度和高度，并可在设置页面即时预览；移动端会自动限制在安全可视区域内。
- 自检缺失或格式错误产生的悬浮红色提示，在用户打开“自检问答”或完整管理器的“当前状态”查看后自动消失；错误详情仍会保留在记录中。

## v0.2.2 更新

- 删除自检预设、单个问题、参考资料库或快捷指令前，都会弹出二次确认，避免误触直接删除。
- 自检问题和快捷指令改为默认折叠的横条列表，展开后再编辑，减少长页面占用。
- 悬浮窗新增“快捷指令”页面，可将已保存指令设为“临时一轮”或“常开”；临时一轮会在下一次生成开始后自动关闭。
- 完整管理器负责创建和编辑指令，悬浮窗负责快速选择启用方式。
- 移动端预设的新建、复制、导出、导入、删除改为紧凑图标按钮，不再纵向占满整屏。
- 参考资料库支持单独导出和导入；导入文件会严格校验格式，并默认保持关闭，防止未知资料立即注入。

## v0.2.1 更新

- 参考资料库改为类似世界书的折叠列表，默认只显示资料库名称与启用状态。
- 新建资料库时必须填写不重复的名称，并选择“文风 / 限制 / 其他”类型。
- 文风资料默认注入主提示词，并自动生成“是否遵照该文风、如何体现”的自检问题。
- 限制资料默认以 System 身份插入聊天深度 0，并自动生成强制限制检查问题。
- 只有启用资料库后，才能开启其对应的自动自检问题；关闭资料库会同步关闭该问题。
- 资料类型、注入位置、深度、角色和自动问题仍可在展开后手动调整。

## v0.2.0 更新

- 支持导出当前通用或角色自检预设，生成可分享的 `.stsc-preset.json` 文件。
- 支持导入别人分享的自检预设，并严格校验文件格式。
- 同名导入会自动重命名，角色绑定、聊天记录与历史自检结果不会被导出。

## 核心功能

- 每轮正文生成前，强制 AI 先完成用户自定义的自检问答。
- 支持开放问答题与判断题，可设置简短、标准、详细回答。
- “提供剧情/设定依据”默认开启，可逐题关闭。
- 通用预设与角色专属预设可单独启用或叠加使用。
- 角色专属预设可绑定当前角色卡，切换角色时自动切换。
- 支持批量粘贴问题，先在临时识别页面调整，再确认导入。
- 支持自检预设和参考资料库导入、导出与分享；资料库可单独导出，也可批量打包为一个合集文件。
- 支持单 API 与双 API 模式。
- 自检结果保存在插件中，不写入聊天正文；开启双 API 复盘时，会将上一轮相关信息提供给自检 API。
- 自检格式错误时显示提示；完全跳过自检时显示红色感叹号。
- 可测试当前生效问题：AI 只回答问题并给出重复、冲突、模糊等建议，不生成正文。
- 内置参考资料库，可保存文风、强制限制和其他长期资料。
- 参考资料可自动追加成自检问题。
- 内置快捷指令库，支持临时一轮或常开，不写入聊天记录。
- 可选悬浮按钮，支持桌面和移动端拖动、查看最新自检、快速切换快捷指令，并自定义悬浮窗样式、按钮透明度、按钮大小与面板尺寸。
- 自动检查插件自身更新；检测到新版本后可在插件“版本更新”页直接完成更新，并在“版本更新”页查看版本号与更新内容。

## 安装

1. 打开 SillyTavern。
2. 进入“扩展”面板。
3. 点击“安装扩展”。
4. 粘贴仓库地址：

   `https://github.com/notyourlittlecheese/SillyTavern-Self-Check.git`

5. 安装完成后刷新 SillyTavern。

## 两种生成模式

### 单 API 模式

只调用一次 API。AI 在同一轮中先输出自检，再输出正文。插件会把自检从聊天消息中剥离，只在管理器和悬浮窗中显示。

### 双 API 模式

在插件设置中填写 OpenAI 兼容接口和密钥、选择模型。独立自检 API 先回答本轮问题，再将结果临时交给酒馆主 API 生成正文。可选择原始问答注入或强力 YAML 规范，并开启上一轮复盘。自检调用、补救重试和正文生成会产生对应 API 用量。

自检失败时，可选择退回单 API 继续正文，或停止本轮生成。复盘需要上一轮成功双 API 来源；回退单 API 的轮次不作为双 API 复盘来源。

## 输出协议

仅当原接口或预设要求可见推理区时，先完整闭合 thinking / reasoning，再输出自检与正文。插件不要求新增或展示内部推理。自检和正文都必须位于推理标签之外：

```text
<thinking>原预设思维链（可选）</thinking>
<stsc_self_check>
<item id="问题ID">
  <answer>最终回答</answer>
  <evidence>具体剧情、角色设定或世界观依据</evidence>
</item>
</stsc_self_check>
正文、状态栏及其他正常输出格式
```

只有勾选“需要依据”的问题强制要求 `<evidence>`。缺少时插件会提示格式有误。

如果推理标签从自检前跨过完整自检块，插件会定向补正边界。如果整段仍被推理标签包住而没有可靠正文起点，插件会保留原文并报警，不猜测分界。正式版使用 `stsc_` 标签；DEV 使用 `stscdev_` 标签，两版各自保留自己的历史记录。

## 推荐隐藏正则（流式模式）

在 SillyTavern 的正则扩展中添加：

```regex
<stsc_self_check\b[^>]*>[\s\S]*?(?:<\/stsc_self_check>\s*|$)
```

替换内容留空。自检结束后，后续正文仍可继续流式显示。

## 注意事项

- 插件检查输出格式与自检完整性，不能保证模型遵循全部指令，也不能证明模型内部推理过程。
- 不同模型的格式遵守能力不同。模型完全跳过自检时，插件会保留正文并显示红色警告。
- “测试预设”、双 API 自检及补救重试会调用额外 API。
- 正式版启用时会关闭同一酒馆中 DEV 的内部开关；建议在扩展页停用 DEV，避免同时操作两版设置。

## 反馈

可在 GitHub 仓库的 Issues 页面提交：

`https://github.com/notyourlittlecheese/SillyTavern-Self-Check/issues`

## 许可证

MIT License
